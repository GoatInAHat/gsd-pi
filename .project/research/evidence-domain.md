# Evidence — domain

<!-- Written by one researcher role. Consumed by the decider. -->

Dimension: domain
Questions assigned:
- `[RESEARCH] Full gap list: every ADR-046 removal gate and invariant not yet satisfied at HEAD, with evidence.`
- `[RESEARCH] Disposition options for the `lifecycle-shadow-no-cutover` gate and `legacy:cleanup:*` scripts after deletion (or after a no-delete ruling).`

Scope note: ADR-046 is the domain contract (`docs/dev/ADR-046-database-authoritative-workflow-lifecycle.md`). All checks run at HEAD `e210e12a41df85b2f02e1de65a2afc37b348ba12` (branch `jeremymcs/path-fixes`). Compatibility-window ship-date math and telemetry-threshold field status are assigned to the `removal-gates` dimension; this file records only what intersects the gap list and gate dispositions, plus the assigned scope-growth risk.

## Finding: step-8 gate "structural no-authority-read tests" — SATISFIED at HEAD

- **Claim**: `node scripts/legacy-state-path-proof.mjs` exits 0 (`Status: PASS`, zero offenders) at HEAD. The proof is symbol-keyed (not specifier-keyed): it bans `_deriveStateImpl` calls/declarations, any `parsers-legacy` specifier, and the relocated `parseLegacyRoadmap`/`parseLegacyPlan` symbols outside their declaration home. Both banned artifacts are confirmed deleted from disk: `src/resources/extensions/gsd/parsers-legacy.ts` does not exist and `_deriveStateImpl` has zero hits under `src/resources/extensions/gsd/state*`.
- **Source**: command run at HEAD (`node scripts/legacy-state-path-proof.mjs` → PASS, exit 0); `scripts/legacy-state-path-proof.mjs:18-40`; `ls src/resources/extensions/gsd/parsers-legacy.ts` → "No such file or directory"; `grep -rn "_deriveStateImpl" src/resources/extensions/gsd/state/ src/resources/extensions/gsd/state.ts` → no hits.
- **Confidence**: high
- **Why it matters here**: This is the strongest satisfied removal gate and the load-bearing evidence for "filesystem-state authority cutover is complete" — the static proof the v1 milestone substituted for never-built `markdownFallbackUsed` telemetry.

## Finding: step-8 gate "production routing closure" — only PARTIALLY satisfied; canonical lifecycle read authority is still legacy by explicit decision

- **Claim**: Filesystem-state routing closure is done (`src/resources/extensions/gsd/state.ts:4` header: "DB-authoritative runtime derivation. Markdown is never a live-path fallback."), but canonical *lifecycle* read authority is deliberately NOT cut over: milestone decision D005 "remains in force for canonical lifecycle read authority", and the `lifecycle-shadow-no-cutover` gate's behavioral witnesses assert that legacy milestone status, dispatch resolution, and validation assessment *remain authoritative* when the canonical lifecycle disagrees (e.g. "legacy milestone status remains public when canonical lifecycle disagrees", "resolveDispatch keeps legacy milestone status authoritative when canonical lifecycle disagrees"). Nine deferred cutover blockers are inventoried in the M003-S07 dossier (`production-read-authority`, `canonical-dependency-eligibility`, `closeout-effects`, `merge-publication-settlement`, `park-unpark-discard-adoption`, `projection-work-redesign`, `legacy-cascade-deletion`, `compatibility-retirement`, `integrated-slice-source-uat-identity`).
- **Source**: `docs/dev/state-db-cutover-milestone-decision.md:17-40` (D005 scope), `:50-60` (nine blockers, "authoritative count: 9"); `scripts/lifecycle-shadow-no-cutover-gate.mjs:94-116` (witness titles); `src/resources/extensions/gsd/state.ts:4`.
- **Confidence**: high
- **Why it matters here**: ADR-046 step 8 requires "production routing closure" before legacy deletion, and Invariant 1 requires the database to be the only normal-runtime Workflow Authority. For the status/dispatch/validation surfaces that closure has not happened and is gated behind a *future separate explicit decision* — this is undone ADR-046 work beyond the visible legacy-import residue, and it directly constrains disposition of the shadow gate (see disposition findings).

## Finding: step-8 gate "telemetry thresholds" — NOT satisfiable at HEAD; the gate fails closed and its counters never measured the ADR-046 paths

- **Claim**: `pnpm run legacy:cleanup:gate` and `pnpm run legacy:cleanup:evidence` both exit 1 at HEAD with "No telemetry file provided. Pass --file or set GSD_LEGACY_TELEMETRY_FILE." The scripts fail closed by design (T015): a missing or stale telemetry file is never zero usage. No CI job, script, or doc at HEAD produces that file; the only producer is the opt-in env var `GSD_LEGACY_TELEMETRY_FILE` read by `src/resources/extensions/gsd/legacy-telemetry.ts:87`, which persists process-local counters on increment/`beforeExit`. The five counters (`legacy.workflowEngineUsed`, `legacy.uokFallbackUsed`, `legacy.mcpAliasUsed`, `legacy.componentFormatUsed`, `legacy.providerDefaultUsed`) measure Phase-8 legacy paths (workflow templates, UOK fallback, MCP aliases, component format, provider defaults) — none measures legacy import machinery usage or markdown fallback. The v1 closeout passed the gate with a locally-generated fresh zero report (`legacy:cleanup:evidence --file <fresh>` then `legacy:cleanup:gate --file <same>` → "PASS (all legacy counters 0; proof zero offenders)"), i.e. the threshold was satisfied by construction on a dev machine, not by field data; residual risk R1 ("No field telemetry for the installed base") was explicitly accepted.
- **Source**: commands run at HEAD (both exit 1); `scripts/legacy-cleanup-gate.mjs:55-58,74-103`; `src/resources/extensions/gsd/legacy-telemetry.ts:23-29,45-52,86-95`; `docs/dev/state-db-cutover-milestone-decision.md:125-133` (R1), `:258` (closeout row).
- **Confidence**: high
- **Why it matters here**: This is direct evidence for the INTENT risk "removal-gate telemetry/performance thresholds may never have been built". The ADR-046 telemetry threshold for *import-machinery* deletion has no instrument and no data; the NEEDS-USER ruling (delete on static proof vs build evidence first) is unavoidable.

## Finding: step-8 gate "performance baselines" — NOT satisfied; the program's benchmark deliverables have no artifacts at HEAD

- **Claim**: The #1411 program requires M3.6 "Projection benchmark proving at least 99.5% current within five minutes and 100% rebuildable" and M9.6 "Dispatch, DB open, projection, kernel tick, and resume performance comparison". No projection-lag benchmark, DB-open/kernel-tick/resume comparison, or rollout-performance gate exists at HEAD. The only performance harness is `scripts/auto-dispatch-baseline.mjs`, a per-unit pre-dispatch *counter* harness for issue #442 (relative before/after counters on a fixture, not absolute latency), with one committed baseline JSON. The "99.5" string survives only as a SQL CHECK-constraint comment in `db-canonical-foundation-schema.ts:20`.
- **Source**: `gh api repos/open-gsd/gsd-pi/issues/comments/4948101799` (M3.6, M9.6 text); `scripts/auto-dispatch-baseline.mjs:1-25`; `ls scripts/baselines/` → only `auto-dispatch-73e20e70.json`; repo-wide grep for projection-lag/benchmark scripts → no gate artifacts.
- **Confidence**: high (absence verified by grep + script inventory; a benchmark could in principle live outside the repo, but nothing references one)
- **Why it matters here**: ADR-046 step 8 lists "performance baselines" as a deletion precondition; they were never built. Same NEEDS-USER ruling class as telemetry.

## Finding: step-8 gates "replacements landed" and "fault and restore gates" — SATISFIED with evidence

- **Claim**: Replacements landed: the DB-authoritative path is live and the legacy filesystem-state read path is deleted (proof PASS; `parsers-legacy.ts` and `_deriveStateImpl` gone). Fault and restore gates exist and pass: `node scripts/workflow-authority-baseline.mjs` (the M0.5 "WA" gate) exits 0 at HEAD — 4/4 invariants PASS including the fault-harness contract and fault-boundary matrix. The import path carries extensive fault/restore coverage: ~31 `legacy-import-*.ts` source modules plus ~45 colocated test files including `legacy-import-restore-drill.test.ts`, `legacy-import-live-restore-fault.test.ts`, `legacy-import-application-fault.test.ts`, and `legacy-import-backup-verification.test.ts`. The `migrate-safety-audit.test.ts` fault-injection suite requires the `--test-fault-injection` native build, which CI runs (`.github/workflows/ci.yml:184,342`) but `build:core` does not.
- **Source**: command run at HEAD (`node scripts/workflow-authority-baseline.mjs` → all PASS, exit 0); `Glob src/resources/extensions/gsd/**/legacy-import-*.ts` (76 files); `evidence-codebase.md` (migrate-safety-audit build requirement); `docs/dev/state-db-cutover-milestone-decision.md:254-259` (wave-4 closeout verdicts).
- **Confidence**: high
- **Why it matters here**: These are the two step-8 preconditions that can be marked satisfied-with-evidence without a ruling — they bound the deletion-gate gap to telemetry, performance, production-routing closure, and the window.

## Finding: compatibility window — import/export machinery fully shipped at HEAD; the wave-4 calendar waiver did NOT cover it

- **Claim**: The explicit Import Preview/Application machinery (`legacy-import-preview-*.ts`, `legacy-import-application*.ts`, `legacy-import-backup.ts`, `legacy-import-live-restore.ts`, `legacy-import-forward-repair*.ts`, `db/writers/legacy-import-application.ts`, etc.) ships intact at HEAD with its full test surface. The v1 milestone's user waiver (2026-08-12, "remaining ≥60-day calendar window waived by the project owner ('finish all waves')") applied to the wave-4 filesystem-state deletions (T020–T023), which were deletions of the *state read path* — not to the import/export compatibility machinery. The ADR-046 window for import/export runs "two stable releases and at least 60 days… beginning when Import Preview and Import Application ship"; the cutover release was v1.13.0 (2026-08-08) with v1.14.0/v1.15.0 since, and HEAD is v1.16.1.
- **Source**: Glob of `legacy-import-*.ts` (finding above); `.project/archive/v1-state-db-cutover/STATE.md` final log entries (2026-08-08 release, 2026-08-12 waiver); `docs/dev/state-db-cutover-milestone-decision.md:223-231,246-250`; `docs/dev/ADR-046-database-authoritative-workflow-lifecycle.md:257-260`.
- **Confidence**: high
- **Why it matters here**: INTENT veto: "No deletion of legacy import machinery before the deferred window/telemetry ruling is recorded." The waiver on record does not authorize this milestone's deletion half; the ship-date/release-count math is the removal-gates dimension's question.

## Finding: Invariants 2, 3, 5, 8 — satisfied with structural/test evidence at HEAD

- **Claim**: Invariant 2 (files mutate authority only via explicit import): satisfied — static proof PASS plus the import machinery being the only ingress. Invariant 3 (revision-checked, fenced, idempotent mutations): satisfied structurally — `db/domain-operation.ts` implements the revision-checked write boundary and the WA fault-boundary matrix passes. Invariant 5 (progress survives restart): satisfied by design evidence — durable attempts (`unit_dispatches` rows per ADR-048), persisted checkpoints; ADR-047's context records the "v1.12.0 durable-attempt authority cutover". Invariant 8 (projection failure observable/repairable, non-authoritative): satisfied — WA `projection-conflict` invariant PASS.
- **Source**: `scripts/legacy-state-path-proof.mjs` PASS; `src/resources/extensions/gsd/db/domain-operation.ts`; `node scripts/workflow-authority-baseline.mjs` output; `docs/dev/ADR-048-unitrun-dispatch-row.md`; `docs/dev/ADR-047-auto-mode-liveness-backstop.md` (context paragraph).
- **Confidence**: medium-high (structural evidence and gates; not a runtime audit)
- **Why it matters here**: Gives the decider the per-invariant green entries the gap audit (success criterion 1) requires.

## Finding: Invariants 1 and 10 — NOT satisfied for the deferred lifecycle surfaces

- **Claim**: Invariant 1 ("The project database is the only normal-runtime Workflow Authority") and Invariant 10 ("Legacy adapters translate into Domain Operations and Kernel Outcomes; they contain no independent policy or authority") are violated by design for status-response, dispatch-resolution, and validation-assessment surfaces: on canonical/legacy disagreement, the legacy result is authoritative today, protected by the shadow gate's witnesses under D005. This is not drift — it is the recorded post-v1 state pending "a future separate, explicit lifecycle read-cutover decision".
- **Source**: `scripts/lifecycle-shadow-no-cutover-gate.mjs:94-116`; `docs/dev/state-db-cutover-milestone-decision.md:26-40,233-244`.
- **Confidence**: high
- **Why it matters here**: The gap audit cannot mark all 10 invariants satisfied; ADR-046 is not fully implemented even after the import machinery question is settled. Finishing ADR-046 completely implies the M003 lifecycle read-authority cutover — a scope decision for the decider/user, not this milestone's automatic residue.

## Finding: Invariants 4, 6, 7, 9 — partially evidenced; the program's own completion audit (M11) never ran

- **Claim**: Invariant 4 (one active Attempt per work item) is addressed by ADR-048's `unit_dispatches`-row identity; Invariant 6 (nothing fabricates completion) lost two filesystem guards by recorded decision R5 (SUMMARY-presence and checkbox checks retired as unfailable — "a settled attempt whose SUMMARY file has been deleted now verifies true"); Invariant 7 (evidence/receipts before unlock) rests on `prepareCloseout`/`settleCloseout`; Invariant 9 (approved human-only taxonomy) is backstopped by ADR-047's liveness invariant. But the #1411 program's Milestone 11 — the "Negative audit proves no disk authority, fabricated completion, unbounded retry, human-first objective UAT, or competing lifecycle loop" plus the requirement matrix (M11.1) and final performance/telemetry/rollback drill (M11.4) — has no artifacts at HEAD and no open tracking issue.
- **Source**: `docs/dev/state-db-cutover-milestone-decision.md:182-221` (R5); `gh api repos/open-gsd/gsd-pi/issues/comments/4948101799` (M11.1–M11.6); `gh issue list --repo open-gsd/gsd-pi --search "completion audit ADR-046" --state all` → no matching issue; `docs/dev/ADR-047/ADR-048` (accepted 2026-08-08 / 2026-08-14).
- **Confidence**: medium-high (absence of audit artifacts verified; per-invariant runtime conformance not directly provable from the repo)
- **Why it matters here**: These four invariants can only be marked "satisfied with evidence" by running the M11-style audits, or must carry an explicit ruling — exactly the gap-audit output success criterion 1 demands.

## Finding: scope-growth risk CONFIRMED — the twelve-milestone program has undone Milestones 9–11 beyond the visible residue

- **Claim**: The #1411 decomposition is twelve milestones (M0–M11). The v1 state-DB-cutover milestone completed the filesystem-state slice of M3 (M3.2) plus wave-4 deletions, but the program's own exit criteria remain open: M9 rollout telemetry/canary/performance gates (M9.1, M9.3–M9.6), M10 "Compatibility retirement and deletion" (M10.1–M10.6, exit requires "WA, TC, verify:merge, packaging, live-workflow, import/restore drills, and deletion proofs pass"), and M11 "Completion audit and release handoff" ("The program is complete only when this audit proves every resolved contract — not when old files merely disappear"). No open GitHub issue tracks M10 or M11; all ADR-046 design issues (#1405–#1415, #1407, #1411) are CLOSED, so the remaining work is untracked.
- **Source**: `gh api repos/open-gsd/gsd-pi/issues/comments/4948101799` (full program text, milestones 9–11 and dependency rules); `gh issue view/list` for #1405–#1415 (all CLOSED); searches "Milestone 10", "compatibility retirement deletion", "completion audit ADR-046" → no open tracker.
- **Confidence**: high
- **Why it matters here**: Direct confirmation of the INTENT risk "research may find more undone ADR-046 work than the visible residue". The decider must choose how much of M9–M11 this milestone absorbs vs. explicitly defers.

## Finding: scope-growth — dangling `semantic-shadow-no-cutover` references at HEAD, flagged in v1 review and never fixed

- **Claim**: `scripts/semantic-shadow-no-cutover-gate.mjs` was split-retired (deleted), but `scripts/m003-s07-dossier-input.ts:19-22` still imports `runSemanticShadowNoCutoverGate` from it, `scripts/__tests__/m003-s07-dossier-input.test.ts:16` imports the same dead module, and `scripts/m003-s07-cutover-dossier.mjs:104-109` inventories the retired command `pnpm run gate:semantic-shadow-no-cutover` (no such package.json script at HEAD). Running the dossier tooling crashes on the missing module. This exact landmine was flagged in the v1 wave-2 review ("live landmine… Recommend a follow-up (fits T021's retirement scope)") and was never repaired.
- **Source**: `ls scripts/ | grep -i semantic-shadow` → no match; `sed -n '15,30p' scripts/m003-s07-dossier-input.ts`; `sed -n '95,115p' scripts/m003-s07-cutover-dossier.mjs`; `grep -n '"gate:' package.json` → only `gate:lifecycle-shadow-no-cutover` (package.json:77); `.project/archive/v1-state-db-cutover/review/wave-2.cycle1.md:106`.
- **Confidence**: high
- **Why it matters here**: A second concrete instance of the scope-growth risk: leftover M003 tooling referencing a retired gate — candidate residue for this milestone's cleanup or an explicit deferral note.

## Finding: scope-growth — CONTEXT.md still denies the completed cutover

- **Claim**: `CONTEXT.md:5-10` states the accepted ADR-046 vocabulary "do[es] not describe current runtime authority until the relevant cutover has completed." For filesystem-state authority the cutover completed 2026-08-07/08 (merge #1627, release v1.13.0) and the closeout record says "Live project state is DB-authoritative" — so the disclaimer is stale for state authority while remaining accurate for canonical lifecycle read authority. CONTEXT.md is the project's domain glossary, making this a domain-contract contradiction, not just prose drift.
- **Source**: `CONTEXT.md:5-10`; `docs/dev/2026-05-03-long-running-refactor-plan-of-plans.md` closeout section ("The state-DB cutover milestone shipped. Live project state is DB-authoritative…"); `.project/archive/v1-state-db-cutover/STATE.md`.
- **Confidence**: high
- **Why it matters here**: Feeds INTENT success criterion 5 (no shipped doc contradicting the completed cutover) with a domain-side instance that needs careful wording: the truth is split (state authority cut over; lifecycle read authority not).

## Finding: disposition — complete consumer inventory for both gate families

- **Claim**: `gate:lifecycle-shadow-no-cutover` has exactly two consumer edges: package.json:77 (definition) and package.json:139 (`verify:pr` = `build:core && typecheck:extensions && test:unit && gate:lifecycle-shadow-no-cutover`). `legacy:cleanup:gate`/`evidence`/`proof` exist only as package.json:80-82 definitions. NO GitHub workflow references any of the four (repo-wide grep of `.github/` → zero hits); CI runs the underlying test suites but never these gates. `README.md:221` documents `verify:pr` (indirect edge to the shadow gate). `scripts/audit-test-confidence.mjs:90-93` names `verify:pr` in its audit matrix. Unit tests `src/tests/legacy-cleanup-gate.test.ts` and `src/tests/legacy-cleanup-evidence.test.ts` exercise the cleanup scripts. The shadow gate's 11 behavioral witnesses are ordinary test files under `src/resources/extensions/gsd/tests/` (e.g. `semantic-shadow-no-cutover.test.ts`, `dispatch-guard-closed-status.test.ts`) that run in the unit suite independently of the gate script — retiring the gate script does not delete the witness tests.
- **Source**: greps of `.github/`, `package.json:77,80-82,139`; `README.md:221`; `scripts/audit-test-confidence.mjs:90-93`; `ls src/tests/ | grep legacy`; `scripts/lifecycle-shadow-no-cutover-gate.mjs:88-116`.
- **Confidence**: high
- **Why it matters here**: Defines the blast radius of either disposition: retiring the shadow gate means editing two package.json lines plus the gate script (and its dangling dossier references); retiring `legacy:cleanup:*` means package.json:80-82, two test files, and three scripts. No CI rewiring needed either way.

## Finding: disposition — retiring the shadow gate without a new explicit decision contradicts a recorded milestone ruling

- **Claim**: The v1 milestone decision record states "Gate retirement never contradicts D005 by silence": the lifecycle-shadow gate carries the retired semantic-shadow gate's lifecycle invariants verbatim, "D005 remains explicitly in force" for canonical lifecycle read authority, and "a future lifecycle read-authority cutover requires its own separate, explicit decision". The plan-of-plans closeout repeats this ("The successor gate keeps those invariants; it does not silently reverse D005"). Therefore: (a) RETIRE is only legitimate after the lifecycle read-authority cutover decision lands, or by an explicit recorded ruling amending D005 — otherwise the retirement silently reverses D005, which the repo's own governance forbids; (b) KEEP costs one verify:pr leg (the gate runs in seconds) and maintains 7 structural import-policy checks + 11 behavioral witnesses that currently pass.
- **Source**: `docs/dev/state-db-cutover-milestone-decision.md:233-244`; `docs/dev/2026-05-03-long-running-refactor-plan-of-plans.md` closeout ("Gate split-retirement" paragraph); gate run at HEAD (PASS, exit 0).
- **Confidence**: high
- **Why it matters here**: This is the binding constraint on INTENT success criterion 4: the shadow gate's disposition is not a free choice — "retired" requires either the deferred lifecycle cutover or a new explicit D005-amending decision.

## Finding: disposition — the `legacy:cleanup:*` gates still guard five OTHER live legacy paths, not just ADR-046 residue

- **Claim**: The five counters the cleanup gates evaluate each still have production call sites at HEAD: `commands-workflow-templates.ts:504,671` (workflowEngineUsed), `uok/kernel.ts:188` (uokFallbackUsed), `bootstrap/db-tools.ts:66` (mcpAliasUsed), `component-loader.ts:265,341` (componentFormatUsed), `model-router.ts:714` (providerDefaultUsed). The plan-of-plans promises these gates as the standing deletion blocker: "Deletion work is intentionally blocked until representative runs pass `npm run legacy:cleanup:evidence` … or an equivalent manually collected zero-use snapshot passes `npm run legacy:cleanup:gate`" and "Legacy paths are removed only after telemetry, tests, and migration notes." So: (a) RETIRE removes the only deletion gate for five still-shipping legacy subsystems (none of which is ADR-046 import machinery) and orphans `legacy-telemetry.ts`; (b) KEEP preserves the gate but it currently cannot pass without a hand-fabricated telemetry file (exit 1), so keeping it as-is means keeping a gate that only passes by construction; (c) a third option exists — keep `legacy:cleanup:proof` (which passes honestly on static analysis) and retire or re-scope the telemetry pair.
- **Source**: `grep -rn incrementLegacyTelemetry src/resources/extensions/gsd --include='*.ts'` (5 modules, 7 call sites, non-test); `docs/dev/2026-05-03-long-running-refactor-plan-of-plans.md:691,745`; `scripts/legacy-cleanup-gate.mjs:10-16`; both gate commands run at HEAD (exit 1).
- **Confidence**: high
- **Why it matters here**: The disposition question is wider than ADR-046: these scripts are the Phase-8 deletion gate for other legacy paths. A "retire after deletion" ruling would leave those paths gate-less; a "keep" ruling inherits a fail-closed gate with no telemetry producer.

## Finding: disposition — no doc promises the gates permanently; promises are scoped to deletion events and D005

- **Claim**: Docs promise the cleanup gates as *deletion preconditions* (plan-of-plans:691,745) and the shadow gate as the D005 invariant carrier *until* a separate lifecycle cutover decision (state-db-cutover-milestone-decision.md:233-244). No ADR or doc found that requires either gate to exist permanently. ADR-046 itself does not name either script — it names removal-gate categories (step 8) and the compatibility window. The gates are implementation machinery whose documented lifetimes end at (a) completion of the deletions they guard and (b) the lifecycle read-authority decision, respectively.
- **Source**: `docs/dev/ADR-046-database-authoritative-workflow-lifecycle.md:253-260` (step 8, window; no script names); `docs/dev/2026-05-03-long-running-refactor-plan-of-plans.md:648-649,691,745-755`; `docs/dev/state-db-cutover-milestone-decision.md:233-244`.
- **Confidence**: high (based on full-text reads of the three governing docs; absence claim bounded to docs/dev, CONTEXT.md, README)
- **Why it matters here**: Frees the decider from "docs promise permanence" — but any retirement must still update these docs' closeout/promise language to stay consistent (doc-drift work feeds success criterion 5).

## Finding: follow-on ADRs 047/048 are operational refinements, not ADR-046 completion work

- **Claim**: ADR-047 (auto-mode liveness backstop, Accepted 2026-08-08) and ADR-048 (UnitRun = the claimed `unit_dispatches` row, Accepted 2026-08-14) both postdate the cutover and harden auto-mode liveness/identity on top of the DB-authoritative base. Neither carries removal gates or deletion preconditions; ADR-048 explicitly deletes RAM identity (`lastAdvanceKey`, `orchestrationUnitPendingCloseout`) as consequences already enacted. They reference the ADR-046 base but add no undone removal-gate scope.
- **Source**: `docs/dev/ADR-047-auto-mode-liveness-backstop.md:1-10`; `docs/dev/ADR-048-unitrun-dispatch-row.md:1-20` (Consequences section).
- **Confidence**: high
- **Why it matters here**: Bounds the scope-growth risk from the "ADR-047/048 follow-ons" thread: nothing in them extends the ADR-046 finish line.

## Assigned questions — answers

- `[RESEARCH] Full gap list: every ADR-046 removal gate and invariant not yet satisfied at HEAD, with evidence.` → Removal gates (ADR-046 step 8): **satisfied** — replacements landed (proof PASS, legacy read path deleted); fault and restore gates (WA baseline 4/4 PASS at HEAD, import fault/restore/backup test suites present); structural no-authority-read tests (`legacy-state-path-proof` PASS). **Not satisfied** — production routing closure for canonical lifecycle read authority (D005 in force, 9 dossier blockers, gate witnesses assert legacy-authoritative disagreement behavior); telemetry thresholds (gate fails closed at HEAD, no producer, counters never instrumented for import/markdown-fallback paths; v1 passed it with a hand-generated zero file); performance baselines (M3.6 projection benchmark and M9.6 five-surface comparison have no artifacts). **Window-gated/deferred** — legacy import/export machinery intentionally retained pending the deferred ruling (window math assigned to the removal-gates dimension). Invariants: 2, 3, 5, 8 satisfied-with-evidence; 1 and 10 not satisfied for the deferred lifecycle surfaces; 4, 6, 7, 9 partially evidenced with the M11 completion audit (negative audit, requirement matrix, final drills) never run and untracked. Program level: #1411 Milestones 9 (rollout telemetry/canary/perf), 10 (compatibility retirement/deletion), 11 (completion audit) are not done; no open issue tracks them. Sources: per-finding above.
- `[RESEARCH] Disposition options for the `lifecycle-shadow-no-cutover` gate and `legacy:cleanup:*` scripts after deletion (or after a no-delete ruling).` → Consumers: `package.json:77,139` (shadow gate, wired only into `verify:pr`), `package.json:80-82` (cleanup trio), `src/tests/legacy-cleanup-{gate,evidence}.test.ts`, `README.md:221` (indirect), `scripts/audit-test-confidence.mjs:90-93` (indirect); zero CI-workflow references; the shadow gate's witness tests live independently in the unit suite. Shadow gate: RETIRE is blocked by the recorded "Gate retirement never contradicts D005 by silence" ruling — legitimate only after the deferred lifecycle read-authority cutover or a new explicit D005-amending decision; KEEP costs one fast verify:pr leg and preserves 7 structural + 11 behavioral checks. Cleanup trio: RETIRE removes the only deletion gate for five other still-live legacy paths (workflow templates, UOK fallback, MCP aliases, component format, provider defaults — 7 production counter call sites) and orphans `legacy-telemetry.ts`; KEEP preserves a fail-closed gate that cannot pass without a hand-supplied telemetry file; a documented third path is keeping the honest static `legacy:cleanup:proof` while retiring/re-scoping the telemetry pair. No doc promises any of these gates permanently — promises are scoped to deletion events and to D005. Sources: per-finding above.

## Dead ends

- `.gsd/DECISIONS.md` — referenced by `state-db-cutover-milestone-decision.md` as the D005 source of truth, but `.gsd/` is absent from this worktree (symlink target lives outside the repo); D005's text was taken from the verbatim quote inside the decision record instead.
- npm registry / GitHub release dates for Import Preview/Application ship release — deliberately not pursued here; assigned to the `removal-gates` dimension (INTENT question 1).
- "99.5% current within five minutes" projection benchmark — the string at HEAD exists only as a SQL CHECK-constraint comment in `db-canonical-foundation-schema.ts:20`; no benchmark harness found under `scripts/`, `src/`, or `tests/`.
- `.project/research/DOCS-AUDIT.md` — checked for gate/doc-drift rows mentioning `legacy:cleanup` or `lifecycle-shadow`; zero hits, so the docs audit adds nothing to the gate-disposition question.
- `scripts/m003-s07-*` dossier tooling — confirmed dangling (imports the deleted `semantic-shadow-no-cutover-gate.mjs`), but it is unreachable from every verify:pr leg and CI; recorded as a finding, not pursued further.
