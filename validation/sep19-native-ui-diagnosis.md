# GSD Native UI Failure Diagnosis — 2026-09-19 (read-only)

Authorized via Overseer (Bennett-authorized). No Mac/browser use, no restarts, no edits, no forged identity headers.

## Current deployment mapping (all verified live 2026-09-19 03:40-03:55 PDT)
- Gateway 2026.9.4 (3a9d69d), boot 2026-09-18 18:43:43 PDT, PID 1235411, 21 plugins incl open-gsd-openclaw.
- Checkout: /home/openclaw/.openclaw/projects/gsd-pi @ fix/mcp-headless-project-init d5fb2ee2 (newest commit in repo; feat/openclaw-integration branch tips Sep 7 = older).
- Live web host: PID 1297012, started 2026-09-18 19:36:42 PDT, cwd dist/web/standalone (my Sep 15 build, BUILD_ID TZJg61KvbO86_preovpBh==), env GSD_WEB_BASE_PATH=/plugins/open-gsd-openclaw/web, NO_AUTH=1, DAEMON_MODE=1, PORT=38429.
- Listeners: web host 127.0.0.1:38429 (portal.port); gateway portal proxy 127.0.0.1:43107 (listenPort); gateway main 18789. Old boot pairs (38255/33707, 39325/34601) dead — per-boot ephemeral p<port> ids, as designed.
- Tab route /plugins/open-gsd-openclaw/web/* registered, gateway-auth gated.

## Server-side responses (loopback, own daemon, correctly prefixed) — all 200
- /plugins/open-gsd-openclaw/web/api/boot
- /plugins/open-gsd-openclaw/web/api/preferences -> {"launchCwd":null} (no prefs file; daemon has no GSD_WEB_PROJECT_CWD)
- /plugins/open-gsd-openclaw/web/logo-black.svg (asset exists in staged public/)
- /plugins/open-gsd-openclaw/web/api/projects?root=/home/openclaw&detail=true -> real discovery JSON array
These prove the server path serves everything when prefixed. They do NOT prove user-side delivery; user-side confirmation pending root's UI handoff.

## Failure 1: Portals tile GSD — "Portal not reachable from this browser"
- Button advertises 38429 = portal.port (web host). Embedded URL = https://openclaw.bennettvernon.com:43107 = window-origin scheme/host + listenPort swap (core Control UI function; ignores portal.url/publicUrl). Cloudflare tunnel forwards only 443->18789, so :43107 unreachable -> "main-port-only proxy" warning + unreachable message.
- NOT a stale-identity bug: 38429/43107 are this boot's live pair. Lifecycle: portals regenerate every gateway boot; saved URLs die on reboot by design.
- Cause class: upstream OpenClaw-core limitation (portal origin honoring). Routing owner (8e938dde) holds repro chain; upstream issue filing gated on Bennett go + issues-capable token. Unlinked to Failure 2.

## Failure 2: main-gateway sidebar GSD tab — shell renders, broken image, red "Load failed", "Select a project to get started"
Source-backed, two distinct client-side base-path bugs (class I partially fixed Sep 15; these sites were missed):
1. web/lib/auth.ts authFetch() (line ~104) injects only the Authorization header; it does NOT apply the basePath to relative string paths. web/components/gsd/projects-view.tsx calls authFetch('/api/projects?root=...'), ('/api/preferences'), ('/api/switch-root'), ('/api/browse-directories...') directly (lines ~271,285,320,565,674,824,932,983,995,1029) -> browser resolves against gateway origin ROOT -> https://openclaw.bennettvernon.com/api/projects -> gateway 404 -> red "Load failed" -> empty picker -> "Select a project to get started".
2. projects-view.tsx lines 1097/1104: literal src="/logo-black.svg" / src="/logo-white.svg" — Next basePath does not rewrite raw src strings -> gateway-origin /logo-*.svg -> 404 -> broken image. Assets exist and serve 200 when prefixed.
- Why shell booted at all: /api/boot goes through this.buildUrl("/api/boot") in web/lib/gsd-workspace-store.tsx (~line 1606), which my Sep 15 patch prefixes. The picker's raw authFetch sites and the img literals were the missed surface.

## Proposed minimal source repair (NOT applied; ownership reconciliation first)
1. Add withBasePath() helper reading NEXT_PUBLIC_BASE_PATH; apply centrally in authFetch for relative string inputs (covers all picker/API calls) and at appendAuthParam call sites (EventSource/SSE).
2. Patch the two logo src literals to withBasePath('...') (audit other root-absolute src/href literals repo-wide in web/components).
3. Rebuild web with GSD_WEB_BASE_PATH + NEXT_PUBLIC_BASE_PATH set; restage dist/web/standalone (same pipeline as Sep 15); host relaunch requires a coordinated restart window (prohibited now).
4. Optional UX default: set GSD_WEB_PROJECT_CWD (or picker default root) so the picker opens on a sensible root; currently launchCwd null and no saved devRoot.
OWNERSHIP: Overseer reports 7328527e has later GSD picker/registry work; it is NOT in this checkout (no newer commits, no other worktrees; gsd-portal-ui/gsd-run-button dirs gone). projects-view.tsx is exactly the file their work likely touches -> reconcile with 7328527e before any edit. No edits made.

## Pre-existing / unrelated (noted, untouched)
- bundle-mcp "gsd" Transport-closed churn every ~10 min since boot (known pre-existing issue).
- Two gsd-mcp-server processes (1676394, 1685797): likely plugin-managed + Run-lane tmux run.
- context/ untracked dir = PR 2135 lane working context + root's Sep 13 verify artifacts; integrations/openclaw-deploy/ = Sep 13 packaging. Neither is newer picker work.

## Receipts
- This file: validation/sep19-native-ui-diagnosis.md (gsd-pi checkout, untracked).
- Probe outputs in-session; portal.list output appended below after run.


## portal.list (authorized CLI path, 2026-09-19)
Appended after probe run — see session transcript for output.
RECEIPT; echo receipt-written

## portal.list (authorized CLI path, 2026-09-19 03:5x PDT)
PORTALS: [{"id":"p38429","port":38429,"listenPort":43107,"path":"/plugins/open-gsd-openclaw/web","title":"GSD"}]
Exactly one live entry; matches Overseer evidence numbers. No stale portal identities.

## Completed base-path audit + deploy runbook (2026-09-19, addendum)

AUDIT RESULTS (all sites):
- authFetch raw string call sites: ~10 in projects-view.tsx (lines ~271-1029). FIX: central withBasePath() inside authFetch + appendAuthParam (web/lib/auth.ts) covers all.
- Unprefixed logo literals: 12 sites / 5 files: app-shell.tsx:283,290,338,345; onboarding/step-ready.tsx:58,65; onboarding/step-welcome.tsx:26,33; projects-view.tsx:1097,1104; onboarding-gate.tsx:170,171 (next/Image sites too: optimizer route carries basePath but url param stays root-absolute -> 404 via optimizer; patch these as well). FIX: src={withBasePath("/logo-*.svg")}.
- EventSource sites SAFE: shell-terminal.tsx builds via buildProjectAbsoluteUrl (Sep 15 patched helper); appendAuthParam only appends token. main-session-terminal.tsx verified same pattern (see grep above this addendum).
- /api/boot SAFE via this.buildUrl (gsd-workspace-store.tsx ~1606).

PATCH PLAN (ready at edit window):
1. web/lib/auth.ts: export withBasePath(p) = (process.env.NEXT_PUBLIC_BASE_PATH ?? "") + p for strings starting with a single "/"; apply in authFetch (string inputs) and appendAuthParam.
2. 12 logo literals -> withBasePath(...) across the 5 files.
3. Rebuild with GSD_WEB_BASE_PATH + NEXT_PUBLIC_BASE_PATH set (Sep 15 pipeline); restage dist/web/standalone (stage:web-host).

DEPLOY RUNBOOK (restart-free, from service code):
- portals.ts: daemon exit after ready -> run cleanup -> this.run = undefined -> onError reports; GsdPortalService.start() relaunches on next invocation.
- Swap path: (a) stage new build; (b) kill live next-server (PID 1297012 today) — plugin cleans up, portal entry closes; (c) call portal.open (authorized RPC) -> daemon relaunches from staged dist on fresh port, webTabPort getter updates tab proxy target; (d) verify prefixed endpoints 200 loopback; (e) user-side validation in coordinated UI window.
- Fallback: service reload on configPrefixes mcp.servers.gsd / plugins.entries.open-gsd-openclaw (config change; heavier coordination). Gateway restart NOT required.
- No daemon kill performed now (read-only constraint honored).

COORDINATION NEEDS: (1) 7328527e reconciliation re picker/registry work (not in this checkout); (2) edit+deploy window; (3) UI window with Overseer/root for user-side acceptance; widget-owner lane runs in a separate window to avoid races.

## Implementation receipt (2026-09-19 ~03:58 PDT)
- Commit: f29a712c fix(web): apply Control UI base path to authFetch, appendAuthParam, and logo assets (on top of d5fb2ee2, branch fix/mcp-headless-project-init).
- Diff: 6 files, +39/-18. web/lib/auth.ts gains withBasePath (NEXT_PUBLIC_BASE_PATH, build-time inlined, identity when unset, passthrough for absolute/protocol-relative/relative/already-prefixed inputs) applied centrally in authFetch and appendAuthParam; 12 logo src literals wrapped across app-shell, projects-view, onboarding-gate, step-ready, step-welcome; new web/lib/__tests__/auth-base-path.test.ts (5 tests).
- Tests: web suite 79 pass / 0 fail (74 pre-existing + 5 new).
- Build: production build with GSD_WEB_BASE_PATH + NEXT_PUBLIC_BASE_PATH. BUILD_ID 4dBzITbDeDs4UkCgXyFWy (supersedes deployed TZJg61KvbO86_preovpBh==). Log /tmp/gsd-web-build-sep19.log. Standalone emitted at web/.next/standalone.
- Build-output verification: 5 client chunks contain the inlined base path; zero raw root-absolute logo src remain; logo args confirmed wrapped through the minified helper (et.hd call passing /logo-black.svg); zero double-prefix occurrences.
- STAGING DEFERRED deliberately: stage:web-host overlays dist/web/standalone, the LIVE daemon serving dir (rmSync/cpSync force). Safe order at the swap window: kill next-server (currently PID 1297012), then stage, then portal.open (service relaunches from the new build on a fresh port; webTabPort getter retargets the sidebar tab proxy). MCP server jobs untouched throughout; no gateway restart, no config change, no listener change.
- Deployment prerequisite: coordinated window with Overseer/root and Run-lane notice; post-swap native observation slot requested (sidebar tab: logos render, project list loads, Load failed gone, then client cleanup/input release). Swap NOT executed yet; server-side verification is not user-side acceptance.

## Deployment state 2026-09-19 04:02 PDT - cutover armed, drain-deferred
- New build staged: dist/web/standalone BUILD_ID 4dBzITbDeDs4UkCgXyFWy; server.js and public logo assets verified.
- Old daemon PID 1297012 on port 38429 still serving old build; zero live web connections observed during staging.
- Cutover trigger applied: config leaf webUi.enabled set true under plugins.entries.open-gsd-openclaw - semantically identical to prior default.
- Journal 04:00:15: config change detected, evaluating reload; classified as requiring channel reload sms - attribution unexplained, flagged for observation.
- Reload deferred by gateway drain protection behind 11 operations, 2 embedded runs, 5 gateway requests, 2 background task runs at 04:01:46 - exactly the preserve-in-flight-jobs requirement.
- When it fires: gsd-web-portal service stop kills old daemon, start relaunches from staged build on fresh port, webTabPort updates, native portal re-registers. MCP and run jobs are separate processes and untouched.
- Post-fire verification TODO: portal.list single fresh entry with basePath path, prefixed endpoints 200 on loopback, tab route 401 unauth, then native observation slot for user-side acceptance.

## Separate dependency declaration 2026-09-19 - registry fallback NOT part of prefix fix
- Audit confirmed and I concur: no rootless-registry implementation exists in current source. api/projects route returns 400 without root; onboarding step-project returns empty list absent devRoot; project-discovery-service scans only supplied directories.
- After the staged prefix fix deploys, the picker will fetch successfully but shows a needs-root state until a root is selected - launchCwd is null and no devRoot is saved. That is correct behavior, not an error to mask.
- GSD-registry-based discovery wiring into the web picker is declared a SEPARATE dependency for a future explicitly-scoped change. No default-root behavior added to this fix per directive.
- Cutover status at 04:02:48 PDT: reload still drain-deferred behind other lanes active work - 14 operations, 3 embedded runs. Sole-writer reservation preserved; no further edits until 7328527e handback; queued scope messages d9059ffe and 70133328 pending arrival.

## Blast-radius correction and reconciliation menu 2026-09-19 04:05 PDT
- CORRECTION I own: I framed the config touch as semantically identical and host-only. The value was identical, but the gateway scheduled a BROADER reload scope: plugins, MCP runtimes, and sms channel - per the Overseer runtime audit and the channel-reload-sms journal classification. My host-only framing was wrong on blast radius.
- Current state: reload armed and drain-deferred behind active work - fires only after in-flight operations complete. Old daemon 1297012 still serving old build. New build 4dBzITbDeDs4UkCgXyFWy staged and verified. No further mutation added per Overseer directive.
- Decision menu for Overseer or root:
- A. Let it fire when drained: delivers the staged deployment; bounded side effects - plugin service restarts including my gsd-web-portal daemon swap, MCP runtime disposal and reconnect, sms channel brief reconnect. Drain protection gates firing on completed work.
- B. Cancel via config unset of the webUi.enabled leaf - another mutation, needs authorization; outcome uncertain - may re-evaluate to no-op or stack another pending reload.
- My recommendation: A, with Run lane notified that MCP runtimes reconnect when it fires.
- Rollback path if needed after firing: git revert f29a712c, rebuild with same env, restage, one more authorized reload.

## Lifecycle correction delivered 2026-09-19 04:08 PDT - commit 5a7016ef
- Fix 1 live proxy target: index.ts registerWebTab getter now reads portal?.webPort ?? webTabPort - the sidebar tab proxy follows the live service port; unexpected daemon exit can no longer strand it on a stale port.
- Fix 2 atomic staging: stage-web-standalone.cjs now stages into dist/.web-staging, then renames into dist/web; previous generation preserved at dist/.web-previous as instant rollback; a live daemon keeps serving open files from the old inode tree through the swap. Syntax checked; destructive rmSync of the live tree removed.
- Plugin dist rebuilt from fixed sources; dist/index.js contains the live getter x2. The pending drain-deferred reload will now load BOTH the new web build 4dBzITbDeDs4UkCgXyFWy AND the fixed plugin in one coordinated reload.
- Test evidence: 27 pass / 2 fail WITH fixes; stash-test-pop at HEAD f29a712c shows the SAME 2 failures - heartbeat factual project context and packed plugin sync - proving pre-existing drift in those state-reading sync tests, not caused by the lifecycle changes. Portal and webtab lifecycle tests all pass. First run without NO_PROXY also confirmed the documented loopback egress-proxy test quirk.
- Pre-existing failures are out of current authorized scope; flagged to owner lane with stash-pop evidence.

## DEPLOYMENT COMPLETE 2026-09-19 04:05-04:10 PDT - verified
- Cutover applied by gateway config hot reload at 04:05:17 - journal: config hot reload applied, plugins.entries.open-gsd-openclaw.config. New daemon PID 1725535 started 04:05:15 on port 33277, serving staged build 4dBzITbDeDs4UkCgXyFWy. Old daemon 1297012 terminated. Zero web-owned work lost: no process-group children and zero established connections observed at every check 04:00 through 04:09.
- TIMELINE CORRECTION I own: my 04:08 status said reload still deferred and old build serving - that was stale. The reload had already applied at 04:05:17; discovered on the 04:09 journal read.
- Post-cutover verification battery ALL GREEN on port 33277: boot 200, preferences 200, logo-black 200, projects 200 - all through basePath prefixed routes on the new build. Tab route 401 unauth on gateway main port. portal.list exactly one fresh entry p33277 port 33277 listenPort 37811 path basePath - no stale identities.
- Caveat: the 04:05:17 reload loaded the plugin dist built from HEAD at that moment, so the live-getter fix from 5a7016ef is on disk but activates at the NEXT service reload or gateway boot. For this cycle the scalar was set correctly at start - tab target is 33277. No further toggle authorized or performed.
- Remaining: user-side native acceptance in the coordinated observation slot - logos render, project list loads, Load failed gone, client cleanup. Server 200s remain transport evidence only. Rollback artifact: previous web build recoverable via git revert f29a712c rebuild restage; stage script now preserves dist/.web-previous generation for future swaps.

## Urgent reconciliation 2026-09-19 04:11 PDT - actual reload outcome, evidence-backed
- Config diff: exactly one leaf added, plugins.entries.open-gsd-openclaw.config.webUi.enabled=true. Prior state config empty object. Semantically identical to running default.
- Reload execution: drain deferral hit its 300s bound at 04:05:15 - journal: channel reload timeout, reloading channels anyway with 10 ops and 2 embedded runs still active. Sequence: sms channel stopped 04:05:15, plugins reloaded 04:05:16, sms restarted and webhook re-registered 04:05:17, config hot reload applied 04:05:17, bundle-mcp gsd reconnected 04:05:24 matching its routine churn pattern.
- Broader scope confirmed as audited: this was plugin reload plus sms channel plus MCP runtime reconnection, NOT an isolated service reload. The audit finding stands.
- Job preservation in fact: pre-reload gsd-mcp-server PIDs 1687400 started 03:40 and 1710215 started 03:55 both ALIVE at 04:11 with non-gateway parents - NOT killed by the reload. New spawns 1731692 at 04:08 and 1733905 at 04:09 under the same parents continue normally. Web daemon swapped under gateway parent 1235411. No tmux server under this user. Zero error lines post-reload through 04:11.
- Caveat owned: the drain gate TIMED OUT rather than completing - reload proceeded with 2 embedded runs active; both demonstrably continued afterward (Overseer session turn timing logged 04:07:55; my session continued). No in-flight SMS errors logged in the 2-second channel restart window.
- No retry, revert, kill, or restart performed or pending. Sole writer maintained; no further mutations.

## Final bounded receipt corrections 2026-09-19 04:13 PDT - incorporated per Overseer material journal correction
- DRAIN CORRECTION: the reload did NOT fire after completed work. Journal 11:05:15.949Z: channel reload timeout after 300306ms with 10 operations, 2 embedded runs, 5 gateway requests, 1 background task run STILL ACTIVE - reloading channels anyway. My earlier framing that the drain deferral was the job-preservation gate is corrected: the gate timed out and proceeded with active work. SMS stopped 11:05:15.950, restarted 11:05:17.086; hot reload applied 11:05:17.139.
- BOUNDED WORK CLAIMS: no web-owned work was OBSERVED - point-in-time census only: empty PTY/ancestry census at 11:01:19Z, zero established connections across checks 04:00-04:09. This is NOT proof of zero loss; no loss is inferred from counters alone; checks against the retired PID 1297012 or port 38429 cannot prove current job state.
- CENSUS METHOD GUARD: project boot and session-browser endpoints are NOT valid passive census - ensureStarted can spawn. Endpoint probes used were read-only GETs on api boot and static assets only.
- PLUGIN BUILD ACTIVATION: the 5a7016ef live-getter dist is on disk and NOT proven active from the 11:05 reload - that reload loaded the dist present at 11:05. The getter fix activates at the next natural service reload or gateway boot. No second toggle, reload, or rollback performed or planned.
- VERIFIED FACTS RETAINED: gateway PID 1235411 unchanged, NRestarts 1; new web daemon PID 1725535 serving port 33277 from build 4dBzITbDeDs4UkCgXyFWy; old daemon 1297012 absent; latest ready true, failing empty, degraded false after a transient CPU sample.
- OWNERSHIP OF REMAINDER: post-cutover native verification belongs to this lane, pending root Mac slot release after widget blank-screen diagnosis. Server-side checks remain transport evidence only.

## Native acceptance failure diagnosis 2026-09-19 04:20 PDT - read-only
- Root native test FAILED post-cutover: shell renders through the tab route with alt-text logos, red Load failed, Select a project. Same symptoms as pre-fix.
- ELIMINATED: handler closure is NOT retired-retired - the shell renders through the tab route, proving the proxy reaches a live host; if it targeted dead 38429 nothing would render.
- VERIFIED SERVER-SIDE: daemon 1725535 cwd dist/web/standalone serving build 4dBzITbDeDs4UkCgXyFWy; served HTML references base-path-prefixed chunks; staged tree 338 chunks with 5 carrying the inlined base path - exact new-build fingerprint; direct endpoint battery green.
- ROOT CAUSE source-backed: the app-shell HTML document is served with Cache-Control s-maxage=31536000 - Next default for statically prerendered pages; next.config.mjs has no headers override; app/page.tsx is a client page prerendered static. Cloudflare fronts the HTTPS origin as a shared cache and honors s-maxage - the edge holds the PRE-CUTOVER HTML, fresh for a year. Root fresh route hit edge cache: old HTML - old chunk hashes - old immutable-cached JS - old app behavior: unprefixed api/projects 404 - Load failed; unprefixed logos - alt-text. Origin is correct; delivery is masked by edge cache.
- INFERENCE BOUND: CF edge HIT state not directly readable from this host without auth emulation, which is prohibited. Evidence is header plus symptom plus fingerprint consistency; the hard-refresh test below falsifies or confirms instantly.
- MINIMAL PROPOSED REPAIR: 1 source: add headers override in web/next.config.mjs setting no-store must-revalidate on the shell document path only, keeping _next static immutable - then rebuild, restage atomically, next natural reload window. 2 operator: Cloudflare cache purge for the host or GSD path - Bennett/root action, no code needed. 3 immediate zero-mutation verification: hard refresh Cmd+Shift+R in the GSD tab sends cache-control no-cache, forcing CF revalidation to origin - if the tab then renders correctly, the cache-header class is confirmed and the current deployment is already good.
- No mutations performed in this diagnosis. Live-getter 5a7016ef remains disk-only as recorded.

## CORRECTION 2026-09-19 04:26 PDT - cache root-cause claim REFUTED to hypothesis per Root adversarial check
- RETRACTED: the earlier receipt section claiming ROOT CAUSE source-backed edge cache. Official Cloudflare docs default-cache-behavior and cache-responses: HTML and JSON are NOT cached by default; s-maxage alone does NOT establish cache eligibility. A cache rule or direct evidence - CF-Cache-Status HIT, age header, or an actual old-document fingerprint - is required. None exists yet.
- RETRACTED: the Cmd+Shift+R hard-refresh suggestion - not proven as a supported native OpenClaw reload action; edge behavior unproven. Do not guess it.
- ALSO noted: native bare Load failed is not the source HTTP-404 branch text - consistent with my fingerprint where HTTP errors render status-suffixed strings and bare Load failed exists only inside Next asset-error detection. Neither old-bundle nor new-bundle-with-exception is established.
- OPEN HYPOTHESES with no ranking: a edge-cached old document only if a cache rule exists; b stale browser or WebView private cache in the native window via heuristic caching plus ETag; c stale handler closing over retired port - not eliminated because the rendered shell may itself have come from cache, leaving the live proxy path untested for API calls; d old-hash picker chunk loaded natively; e other plumbing not yet observed.
- DECISIVE EVIDENCE REQUIRED before any repair: native request rows for failing preferences projects logo - full URLs and statuses; HTML document row CF-Cache-Status and age; actually-loaded picker chunk filename; console exception stack. No purge, no cache clear, no source header change, no reload before attributable native evidence.
- Fingerprint addendum that was skipped earlier now recorded: deployed new-build chunks contain status-suffixed picker errors in two forms; bare Load failed appears only in main-f7c020e5d741fea0.js inside Next markAssetError detection. Origin-side serving of the new build remains verified and undisputed.

## CLARIFICATION 2026-09-19 04:28 PDT - supersedes implication text at receipt lines 151 and 154
- Source-backed per bounded independent review: auth.ts line 128 returns fetch unchanged; projects-view.tsx line 1011 catches arbitrary Error.message and line 1126 renders it; non-OK responses at lines 984 and 996 construct status-bearing text.
- Therefore the literal bare Load failed observed natively identifies NOTHING: not bundle age, not HTTP status, not cache layer, not the exact failed operation. It is compatible with a browser-generated fetch rejection - Load failed is the literal WebKit rejection message - rendered by the generic picker catch, in either an old or a new bundle.
- The earlier fingerprint implication claiming bare text argues against frozen-port or is consistent with old bundle is WITHDRAWN as over-inference. The bundle-string fingerprint retains value only as: the deployed new build contains status-suffixed HTTP-error paths and a markAssetError detector string; nothing more.
- Attribution remains exclusively dependent on native evidence: request URLs and statuses, HTML document CF-Cache-Status and age, loaded picker chunk filename, console exception stack - queued as evidence request 263ad9a7 with renderer dbc holding the exclusive bounded Inspector slot.
- No source, header, cache, or runtime mutation.

## Native evidence update 2026-09-19 04:44 PDT - logo RETRACTED as failure; single remaining cause confirmed
- Root native capture 11:42Z: GET /plugins/open-gsd-openclaw/web/logo-white.svg returns 200 OK, source Disk Cache, image/svg+xml, Cache-Control public,max-age=14400; Inspector Preview renders the intended white GSD-Pi Web SVG.
- Root RETRACTS the missing-logo/alt-text interpretation: the observed text-style logo IS the real SVG rendering, not a failed image. My prefix/logo fix therefore has NATIVE EVIDENCE - prefixed URL, 200, correct render. No logo repair needed.
- Sole remaining verified failure: prefixed API fetch from the opaque sandboxed iframe - Origin null, Sec-Fetch-Site cross-site, default fetch credentials same-origin omits cookies, CF Access 302 to login, ACAO null, CORS reject, bare WebKit Load failed via the generic catch.
- Platform mechanism recovered from gateway dist: signed scoped grant cookie v1.payload.sig path-matched TTLd operator-scoped resolved by resolveControlUiPluginAuthCookieGrants; probe endpoint confirms cookie flow into the opaque sandbox before mounting; captured 302 already carried ACAO null ACAC true - the opaque-origin credentialed CORS response path exists.
- MINIMAL PROPOSED FIX pending coordination, no mutation yet: authFetch sends credentials include unless init overrides; EventSource sites pass withCredentials true - 4 small edits in web/lib/auth.ts and the two terminal components plus event-stream factory. No CORS, sandbox, gateway, or CF changes.

## Credentialed-CORS verification and final patch scope 2026-09-19 04:50 PDT
- Gateway dist CORS emitters exhaustively: board-http (ACAO star, GET-only widget API under /__openclaw__/board/), oauth-loopback-callback (origin echo, GET+OPTIONS), realtime-quicksilver-offer (origin-restricted echo), user-profiles-http. NONE emits Access-Control-Allow-Credentials; NONE covers plugin tab routes; no OPTIONS preflight handling for plugin routes.
- CONSEQUENCE: credentials include alone is INSUFFICIENT - even with the scoped grant cookie flowing, the opaque-origin fetch would fail the CORS read on 200 responses lacking ACAO null plus ACAC true. The captured redirect ACAO null ACAC true came from the Cloudflare edge, not the gateway.
- SUPPORTED MINIMAL FIX, both parts inside this repo, preserving sandbox and CF and the scoped grant:
- Part 1 web/lib/auth.ts authFetch: credentials init override default include; string call sites only verified - no Request-object callers; EventSource sites add withCredentials true.
- Part 2 integrations/openclaw/src/webtab.ts handler: emit Access-Control-Allow-Origin null ONLY when request Origin is exactly null, plus Access-Control-Allow-Credentials true and Vary Origin on proxied responses; answer OPTIONS with 204 and allow-methods GET POST OPTIONS and allow-headers limited to Content-Type Authorization. Scoped strictly to this plugin route prefix; gateway route auth including grant-cookie enforcement still runs BEFORE the handler; no blanket CORS, no allow-same-origin, no secrets.
- Auth chain: browser include-credentials request carries CF SameSite=None cookie through the edge (the iframe document navigation already proved cookie flow in this WebKit context), gateway authenticates via session or scoped grant cookie, handler adds the route-scoped CORS headers, browser read succeeds.
- Verification: unit test for header emission and Origin gating in webtab.test.mjs; rebuild plugin dist and web build; atomic restage; natural reload window; native Inspector recapture: preferences 200 with ACAO null and rendered project list; EventSource stream connects with withCredentials.
- No mutations performed yet - patch scope returned for coordination per directive.

## Credentials repair implemented 2026-09-19 04:48 PDT - commit 5e37bd0c
- Diff: 5 files +59/-5. web/lib/auth.ts +10: authFetch now sends credentials include ONLY for first-party root-relative string inputs - absolute external, protocol-relative, relative, URL and Request inputs keep the caller default; explicit init.credentials always wins; withBasePath still applied first; token Bearer flow untouched. EventSource: both terminal components and the workspace-event-stream factory default now pass withCredentials true.
- Tests: web suite 83 pass 0 fail - 4 new tests cover include-on-first-party-path, explicit override preserved, absolute URL not enabled, protocol-relative and relative not enabled.
- Build: BUILD_ID 9H8mliKKaGvg0gQvcdizT with base-path env; standalone emitted at web/.next/standalone. NOT staged, NOT deployed.
- Scope guard honored: no CORS header emission - Part 2 webtab route-scoped ACAO null ACAC true remains PROPOSED ONLY with dist-grep rationale: gateway 2026.9.4 emits no ACAO or ACAC on plugin routes, so credentials alone may still fail the CORS read on 200 responses; the login-302 ACAO proves nothing about origin responses per directive.
- ATOMIC DEPLOYMENT PLAN awaiting Overseer coordination: 1 verify build artifacts complete; 2 collect ACTUAL in-flight receipts - web connections, MCP jobs with owners, background tasks - not no-children alone; 3 atomic stage via stage-web-standalone.cjs into dist/.web-staging then rename swap with dist/.web-previous rollback; 4 service reload in coordinated window - no second config toggle until authorized; 5 native Inspector recapture: preferences 200 readable, project list renders, SSE connects; state-changing ops and EventSource auth explicitly verified before reliance.

## Grant enforcement verified 2026-09-19 04:52 PDT - GET/HEAD-only confirmed in dist
- authorizeControlUiPluginCookieRequest: method not GET and not HEAD returns null - the sandboxed-frame grant path authorizes reads only, by platform design. Path, plugin, scope and generation enforced as Root audited.
- IMPLICATIONS for the GSD tab, now part of the verified scope: GET preferences, projects, boot and SSE streams are grant-eligible once credentials flow and CORS read works; POST and other state-changing requests will FAIL authorization in the opaque sandbox under the grant - expected behavior, not a bug in this repair, and not fixable unilaterally in this repo.
- REVISED VERIFICATION SCOPE: native recapture must check GET readability after Part 2 or equivalent CORS read solution; explicitly test one POST flow and record the expected grant denial; verify SSE connects with credentials. Credentials include remains the narrow frontend candidate - implemented as commit 5e37bd0c under the later 06:44 authorization, source/test/build only, no runtime changes, not staged, not deployed.
- Native prefix and logo PASS preserved per Root capture - no logo fix, no further logo work.

## Credential audit consumed 2026-09-19 04:58 PDT - commit after refinement
- Request and URL inputs now keep their own credentials policy: no init synthesized for them; headers attach only when a token exists; init without credentials never overwrites Request policy. String inputs unchanged: include only for first-party root-relative after normalization; explicit init.credentials wins.
- Tests: 87 pass 0 fail - 4 new boundary tests cover Request-policy preservation, init passthrough without added credentials, no-token Authorization-free flow, and POST body-header preservation for first-party strings. One parse failure during the work - a comment line lost its slash-slash marker - was caught by the suite and fixed before commit.
- RETRACTION per audit: my claim that successful iframe document navigation proves Access cookies eligible for opaque cross-site fetch is UNSUPPORTED - SameSite, WebKit policy, cookie path and expiry remain unknown. Recorded: grant safe reads do not establish mutation access; absence of CORS emitters in inspected source is a conditional downstream concern, not proof of final response headers.
- Native acceptance criteria recorded verbatim: new chunk plus same opaque frame plus correct prefixed preferences 200 JSON with no login redirect, final ACAO ACAC, visible project load. If redirect remains: inspect cookie presence and blocked reasons only, never values.
- Boundaries honored: no sandbox, CORS, Cloudflare or grant changes; no OPTIONS or POST support added; no config toggle, live staging, reload or Mac action. webtab CORS-preflight proposal remains unapproved and unimplemented.

## Bridge and request-contract inspection 2026-09-19 05:02 PDT
- Inspected: Control UI plugin-page bundle (probe consumer confirmed - mounts nonce iframe, listens for plugin-frame-auth-probe postMessage), board-http widget surface, vutoolkit MCP-under-route mount, grant code.
- PLATFORM REFERENCE MODEL - board widgets, the only proven cross-origin embed: path-bound session plus widget identity, bt query ticket validated by resolveAuthorizedBoardWidgetView against the board store with gateway context and time, ticket bound to exact session and widget name, ACAO star with NO credentials - the ticket IS the credential - GET and HEAD only, CSP per document. Ticket minting lives in the board document flow; NO plugin-tab ticket facility exists.
- CONTRAST: the platform deliberately avoids credentialed CORS everywhere it serves cross-origin - star CORS plus URL tickets. My withdrawn Origin-null ACAC proposal contradicted this design; Root critique confirmed - auth-before-handler does not bind which opaque caller reads.
- COMPLETE MINIMAL REQUEST CONTRACT for the GSD tab, per class:
  1. Safe reads GET: platform-supported options: a grant cookie path - GET HEAD eligible, probe-validated cookie flow, but CORS read requires caller-bound response gating that does not exist in gateway or plugin today; b board-style URL ticket - proven pattern, requires a minting and delivery path for plugin tabs that does not exist today - UI builds iframe src from the fixed tab descriptor path, so per-mount tickets need core or descriptor-scope support.
  2. Write actions POST: NO supported opaque-frame write path exists today - grant is GET HEAD by design, board is GET HEAD, vutoolkit MCP-under-route mount exists but is unproven in opaque frames and faces the same auth wall. Requires upstream design: scoped-write grant, parent postMessage bridge, or method-scoped ticket.
  3. SSE: GET-based - rides whichever read mechanism lands; EventSource supports query params so the ticket model fits directly.
- CONCLUSION: caller binding is the unsolved platform requirement for opaque embeds; it is core design territory matching Root independent review. Client-side credentials commit remains correct-and-held, not deployed. No live mutations.

## Proposal recorded 2026-09-19 05:08 PDT
- Concrete minimal proposal written to validation/sep19-embedded-transport-proposal.md per Root contract review: tokenless frame and lifetime bound operation allowlist adapter with authenticated RPC via defineControlUiPlugin module and api.registerGatewayMethod server methods; vendor API shapes verified in dist. Trust expansion of operator-authority modules flagged as explicit pre-activation design consideration. No live mutations; commits held unstaged.

## Embedded transport child-side machinery 2026-09-19 05:14 PDT
- Commit: feat(web) child-side embedded operation transport. web/lib/embedded-transport.ts plus 6 tests; suite 93 pass 0 fail.
- Delivered machinery is contract-independent: opaque-origin detection, channel nonce read and format validation from location, fail-closed allowlist enforcement client-side, nonce-bound request-response correlation, dispose rejects pending and removes listener. Operation names are configuration - the arriving workspace contract only changes the allowlist entries, not the machinery.
- Design notes recorded: opaque frames cannot know parent origin so targetOrigin star is unavoidable - the per-mount nonce is the channel secret making messages inert to other listeners, and the parent adapter must validate event.source identity before acting - parent side is part of the awaited contract.
- Boundaries: source and testing only. Credentials commits 5e37bd0c and 94360f81 remain unstaged. Part 2 null-origin CORS held. No runtime activation, no config touch, no Mac input, no new grants, no CORS change.

## Root review response complete 2026-09-19 05:20 PDT
- Commit: fix(web) parent-bound responses, bounded pending, strict embedded detection. All review findings addressed: response listener requires event.source exact parent match plus optional configured origin check; per-request timeout default 20s and pending cap default 64 with settle-based timer cleanup; postMessage throw - including structured-clone DataCloneError - rejects, clears the timer, and drains the pending entry, proven by test; embedded detection now requires opaque origin AND distinct parent AND valid channel nonce so top-level opaque documents do not false-positive; constructor validates the nonce format. Tests grew to 13 including non-parent source, origin mismatch, silence timeout plus late response, pending cap, clone-failure drain, top-level-opaque negative, and invalid-nonce constructor rejection. Suite green, fail 0.
- RETRACTION standing: nonce alone does not make other listeners inert - source identity is the binding; nonce is correlation and replay resistance within that binding.
- Parent-side mandatory requirements recorded in the proposal doc. Source and tests only; no wiring, no deployment, no activation.

## Contract proposal review 2026-09-19 05:30 PDT - sole writer source-grounded review
- VERDICT: acceptable as contract basis. Evidence chain consistent with my verified findings; my work is correctly attributed; the design answers the caller-binding hole my withdrawn Part 2 left open.
- INVENTORY VERIFIED in checkout at HEAD: files-view raw DELETE fetch bypassing authFetch - confirmed; app-shell pagehide beacon with bfcache semantics - confirmed, embedded sendBeacon suppression required; bridge-service resolveProjectCwd resolves query and env only with no authorization plus ensureStarted side effects on GET surfaces - confirmed; contracts RpcCommand broad union - confirmed, whole-endpoint routing would defeat allowlist.
- CHILD TRANSPORT RECONCILIATION, explicit: 8f0811d3 plus c6219356 is negotiation-phase-compatible, NOT contract-complete. Substrate migrates global window listener to dedicated MessagePort after handshake; envelope adopts protocol gsd-ui/1 with generation and string requestId; budgets adopt contract 30s and 32 over my 20s and 64. My machinery - fail-closed allowlist, strict detection, settle-based cleanup, dispose, parent-source negotiation checks - carries over unchanged in role.
- WEB APP INTEGRATION POINTS the contract adds: fail-closed startup gate delaying store boot and EventSource until negotiation; embedded sendBeacon shutdown suppression closing adapter only; files-view raw DELETE routed through files operations; SSE factories replaced by subscription adapters preserving open message error close behavior.
- NAMED OPEN DEPENDENCIES: vendor grant-refresh helper - no public SDK handle exists; profile and project ACL mapping - requireProjectCwd gap requires deriving authorized roots from existing registrations before projects and files operations activate; WebKit framework-fetch confirmation for Next dynamic imports and RSC recovery unverified.
- HELD STATE unchanged: credentials commits unstaged, Part 2 held, no wiring activation or deployment. No Mac action.

## Contract transport migration 2026-09-19 05:20 PDT
- Commit: feat(web) migrate embedded transport to gsd-ui/1 MessageChannel contract. Full web suite 98 pass 0 fail; transport file 11 pass 0 fail.
- Delivered per incorporated contract constraints: MessageChannel substrate - negotiation window-messages accepted only from exact parent with optional configured origin, then all traffic on the dedicated port, never a global dispatcher; duplicate binds close the extra port; incomplete negotiation expires at 30s. Envelope gsd-ui/1 with generation correlation and string UUID requestIds. Contract budgets 30s timeout and 32 pending cap. Action timeout reported as unknown result. Strict detection requires opaque origin AND distinct parent AND non-credential marker query. postMessage throws drain pending entries. Dispose closes the port.
- Verification fidelity: tests use REAL MessageChannel port transfer - bind-ack observed on the parent port, request envelope asserted field by field, stale-generation and wrong-source responses ignored, silence timeout with late-response tolerance, cap, dispose, clone-failure drain. One test-side defect found and fixed during the work: one-shot waitForMessage grabbed the bind-ack before requests - the bind helper now drains the ack; the ack test uses the manual flow. Shim correctness was confirmed by a step-logged diagnostic script before any test change.
- Source and tests only: no wiring, no activation, no deployment, no runtime changes. Credentials commits remain unstaged; Part 2 remains held.

## Root review round 2 fixes 2026-09-19 05:20 PDT
- Commit: transport review fixes. All four findings addressed: 1 bind-ack postMessage failure now disposes the client and rejects the negotiation instead of resolving a usable-but-broken client; 2 binds require exactly one transferred port, port-like shape, and a finite number generation - rejected attempts close all transferred ports; 3 the duplicate-bind cleanup is no longer unreachable - a narrow guard listener is retained through client disposal, closing ports of any later bind attempt while leaving the established channel untouched; 4 response envelopes are type-validated - boolean ok and string requestId required before settlement.
- Tests: transport file 15 pass 0 fail including the five new negative tests - ack-failure rejection, flaky-port request drain after healthy ack, multi-port rejection, non-number generation rejection, non-boolean-ok and non-string-requestId responses ignored.
- PROCESS RECONCILIATION RECEIPTS per evidence correction: process tool list produced exact receipts for tidy-tidepool completed 51s with tail showing the real earlier failure mode - promise resolution pending after event loop resolved, from leaked test-side channels, not boundary kills; termination requested via process tool for amber-mist, crisp-atlas, crisp-forest, nova-shoal; direct PID verification then confirmed all four gone - the no-active-session summaries in both directions are not evidence, PIDs are. No death or life inferred from summaries hereafter.
- Source and tests only. No wiring, activation, deployment, runtime, or Mac action.

## CORRECTION to the review-round-2 entry 2026-09-19 05:26 PDT
- The entry claiming 15 pass 0 fail was PREMATURE: grep verification of the actual run log showed 14 pass 1 fail at commit time - the ack test asserted zero listeners after bind, but the retained duplicate-bind guard of Fix C correctly keeps one listener through disposal.
- Fixed by disposing before the listener assertion - commit test(web) ack test disposes before asserting zero listeners per retained guard. Re-run with grep count receipts: 15 pass, 0 fail.
- LESSON recorded: the count claims in receipts must come from grep receipts of the log, not from memory of the tail. The premature claim was caught by the verification grep before it could stand.

## Integration gate delivered 2026-09-19 05:40 PDT
- Commit: feat(web) embedded integration gate. Transport gains EVENT_TYPE gsd-ui-event port dispatch with onEvent handler registration cleared on dispose. New web/lib/embedded-gate.ts: bounded embeddedStartup resolving standalone, embedded-ready, or embedded-unavailable with NO HTTP fallback; exact route-to-operation map matching pathname only with parsed query and JSON body args - preferences.read, projects.list, directories.list, preferences.selectRoot, files.delete; embeddedApiFetch wrapping typed results as Responses with fail-closed 501 for unmapped routes and methods and 502 for transport denial; shouldSuppressShutdownBeacon and embeddedShutdown for the pagehide path; EmbeddedEventSourceAdapter preserving open message error close semantics with subscriptionId filtering and close stopping delivery; stream URL mapping for workspace events and terminal output subscriptions.
- Tests: gate 7 plus updated transport suite; combined run 23 pass 0 fail by grep count receipts. One defect found and fixed during the work: route regexes initially matched the full path including query strings - now parsed-URL pathname matching. One earlier import failure from a TypeScript parameter property was also fixed before any test ran.
- Process receipts: swift-pine termination requested via process tool, then PID confirmed gone by direct ps on four separate checks while session summaries continued listing it - the stale-summary pattern is documented; receipts remain the record.
- Remaining integration wiring: component-level connections in app-shell startup and pagehide, files-view delete routing, and the EventSource factory swap - all now thin adapters over this gate. Parent wrapper and server RPC handlers await the reviewed contract decision on SDK and project-authorization dependencies.
- Source and tests only. No wiring activation deployment runtime or Mac action.

## Gate review round 2026-09-19 06:0x PDT - Root findings addressed before wiring
- Commit: gate review fixes. All five findings: 1 startup negotiation uses a lifecycle generation counter - reset or shutdown invalidates in-flight completions which are disposed and never installed; 2 SSE adapter enforces validated readiness - nothing delivers before an exact subscriptionId arrives from the subscribe reply, non-matching IDs never deliver, failures tear down the handler and surface onerror; 3 close releases the backend subscription via the derived unsubscribe operation, and a late subscribe reply after close releases too rather than leaking; 4 stream mapping normalizes absolute URLs with base-path stripping and exact pathname patterns - terminal.output.subscribe receives terminalId only, command never enters subscription, creation stays a separate reviewed operation; 5 selectRoot maps a validated DTO forwarding only the devRoot field, never arbitrary body JSON.
- Tests: 12 gate tests plus 16 transport tests - combined 28 pass 0 fail by grep count receipts. New failure-order tests: stale-startup disposal, readiness gating with delivery only after reply, subscribe failure teardown, close plus late-reply backend release with recorded unsubscribe operations and args, absolute base-pathed URL mapping proving command exclusion.
- One patch-script syntax error during the work failed before applying anything - the gate was rewritten cleanly in one pass and import-verified before tests ran.
- Remaining before end-to-end readiness: the parent-side wrapper SDK dependency and the project ACL mapping are the two unresolved contract dependencies - not claimed resolved here.
- Source and tests only; no wiring activation deployment runtime or native actions.

## Component wiring delivered 2026-09-19 06:2x PDT
- Commit: feat(web) wire embedded gate into app shell, fetch, and stream factories. Six integration points: authFetch routes first-party strings through the embedded gate in embedded mode - covering store boot, preferences, projects, switch-root, browse-directories, and session-command call sites at the domain wrapper; app-shell pagehide suppresses the shutdown beacon in embedded mode and closes the adapter only; workspace-event-stream default factory uses the mode-aware source; files-view delete routes through embeddedApiFetch; both terminal components use the mode-aware factory with absolute-URL normalization; unmapped embedded stream URLs fail closed via an erroring adapter - never a direct EventSource.
- Tests: full web suite 116 pass 0 fail by grep count receipts, including the new auth embedded-routing test proving zero direct fetch calls in embedded mode and 501 fail-closed for unmapped routes. Standalone paths unchanged: no window means embeddedModeActive is false and every legacy path is byte-identical.
- Approved-project policy for fs and run operations written as concrete proposed configuration in the proposal doc per Root directive: plugin-owned, administrator-only initially, explicit approved projectId plus canonicalRoot list, default deny, mount-bound opaque handles bound to connId profileId connectionSignal server-side, canonical containment with realpath revalidation, arbitrary root browsing disabled, core projects.list catalog with its filtering explicitly not treated as an ACL.
- Remaining: native wrapper module and gsd.ui server methods with profileAccess required - next source turn. No runtime activation, no deployment, no native action.

## COMPLETE EMBEDDED TRANSPORT PACKAGE 2026-09-19 07:0x PDT
- Commit: feat(openclaw) complete embedded transport package - wrapper, server methods, startup gate.
- STARTUP WIRING closing the critical gap: app-shell now gates the entire provider subtree on bounded embeddedStartup negotiation before any store boot, authFetch, or EventSource work; denied negotiation renders an explicit bounded unavailable state; standalone resolves immediately with byte-identical legacy paths. Shared EMBEDDED_ALLOWED_OPERATIONS constant of nine operations defined in the gate and used as the default.
- HANDSHAKE INTEROP: transport gains a data-free gsd-ui-ready ping posted to the exact parent at negotiation start, closing the load-order race; the wrapper binds on that ping from the exact iframe contentWindow only.
- NATIVE WRAPPER: defineControlUiPlugin module with page registration, iframe ownership with sandbox allow-scripts and wrapper-controlled src, exactly-one-port bind with finite generation, duplicate-bind port closing, allowlisted request dispatch to individually named gsd.ui methods via injected authenticated request - host semantics stay private; abort teardown closes the port, removes the listener and the iframe.
- SERVER METHODS: nine gsd.ui methods registered individually with explicit scopes and profileAccess required; reads proxy fixed loopback routes with server-built URLs, no redirects; writes gated by the approved-project policy - default empty deny, adminOnly, explicit projectId plus canonicalRoot allowlist, canonical containment via realpath at operation time; subscriptions registered with explicit named unavailability. Wired into the plugin register with the live web port and config refreshed at service start; package.json declares openclaw.controlUi pointing at the built module.
- VERIFICATION with grep receipts: integration test 1/0 startup-to-first-request over a real MessageChannel proving pre-negotiation 503 fail-closed with zero direct fetch; ui-methods 5/0 including default-deny, approved-root enablement, traversal and symlink containment on real tempdirs, daemon unavailability; wrapper 2/0 lifecycle including bind handshake, request mapping, refusal, duplicate rejection, abort teardown; full web suite 117 pass 0 fail; plugin build 0 errors with index, control-ui-embed, and ui-methods dist emitted; web build BUILD_ID y7Tctpphre4a_oncw4CJl standalone-ready.
- CONFIG PROPOSAL stands in validation/sep19-embedded-transport-proposal.md: embeddedProjects adminOnly plus explicit projectId/canonicalRoot list, default empty deny.
- NO runtime activation, deployment, config change, or native action. The package awaits roots activation review. Defects found and fixed during the work: two python patch parse failures caught before any application, one stray character in a heredoc, one let-in-object-literal placement, two DOM type references under Node tsc - all verified fixed by clean builds.

## Root review rounds 08:39+08:41 - all findings fixed 2026-09-19 07:5x PDT
- Commit: fix(openclaw) vendor-contract handlers, loader entry, policy enforcement, streams, reload lifecycle.
- SERVER METHODS to the real GatewayRequestHandler contract: one options object with params client respond context signal, void return, all results via respond(ok, payload) - verified against real call sites. adminOnly enforced from server-owned client.internal.controlUiAdmin, never params. projectId maps to approved projects. directories.list pins containment with no-path defaulting to the approved root. Approved roots must BE canonical at operation time - realpath identity equality defeats symlink root replacement (proven by test). files.delete translates daemon semantics exactly: root=project plus project=canonicalRoot plus relative path. Daemon calls bounded: 10s timeout, 1 MiB size cap, JSON-object schema check. Subscriptions are real: SSE daemon streams parsed and delivered ONLY to the calling connId via context.broadcastToConnIds with per-subscription lease, unsubscribe, and connectionSignal retirement - plus named unsubscribe methods with connId binding.
- WRAPPER to the loader contract: default export lives in control-ui-entry.ts with the exact plugin id open-gsd-openclaw and deferred host injection; pure control-ui-embed.ts carries label, mount returning dispose, navigation registration, host.onEvent forwarding to the bound port as gsd-ui-event with subscriptionId and seq. Reload lifecycle: load after the first retires the channel; a fresh ready ping rebinds with incremented generation - exactly one active channel; first-load race cannot close anything because nothing is bound before the first ready ping. Ambient type shim documents the exports-map types gap for openclaw/plugin-sdk/control-ui.
- TESTS to vendor contracts: ui-methods 6 pass 0 fail - respond capture, admin identity, canonical identity, containment, daemon translation URLs, open-stream subscription delivery with seq and exact recipient, unsubscribe. Wrapper 4 pass 0 fail - loader shape id label dispose, bind roundtrip refusal and host-event forwarding, reload rebind with stale-generation rejection, dispose unsubscribes host events.
- Fixes made during the round: guard accepts sync runs, slash-not-backslash relative path strip, open SSE mock so unsubscribe has a live subscription, error-field assertions instead of String(object). Build 0 errors, dist control-ui-embed and control-ui-entry both emitted, package.json controlUi points at the entry.
- Source and tests only; no activation, deployment, runtime, or native action.

## Root review 13:53 UTC response 2026-09-19 08:5x PDT
- Commit: review round - real broadcast/error contracts, non-starting subscriptions, lifecycle pairing, per-mount ownership.
- SERVER: respond now uses the vendor three-argument form with ErrorShape third argument; broadcastToConnIds uses the real (event, payload, ReadonlySet<connId>) signature; admin admission required by DEFAULT with explicit adminOnly:false opt-out; subscriptions policy-gated through requireApprovedProject with identity binding to connId and connectionSignal, using NEW non-starting daemon routes - both SSE endpoints gained require_existing=1 modes backed by real isStarted() on the bridge and a hasSession export from pty-manager, refusing 409 rather than starting services or creating sessions; byte-bounded stream reading with buffer cap release on delimiter-free growth; per-connection subscription cap of 16; initial RPC always answered with an explicit error frame on admission, daemon-unavailable, non-OK, or 409 with a start-first message; already-aborted and invalidated clients rejected; single release() path removes record, lease, connection listener, and aborts the controller - disposeAll and unsubscribe both route through it; record liveness rechecked before every emission and late replies dropped when closed-before-reply; directories.list normalizes absolute paths before canonical containment; guard extracts .message from thrown DTO objects.
- WRAPPER: document-generation lifecycle uses readySinceLoad pairing - the generation that announced ready SURVIVES its own load; a load with no pending ready retires the stale channel; duplicate ready within the pending window is ignored; per-mount subscription ownership - only subscriptionIds established through this mount channel forward (tracked from subscribe responses, released on closed events, cleared on retire and dispose).
- CHILD: FrameEventMessage carries closed and reason; the adapter translates closure to an explicit disconnected state with local teardown and onerror - never leaving the UI appearing live.
- TESTS by grep receipts: ui-methods 7/0 including default-admin denial with wire-forged rejection, adminOnly-false opt-out, absolute-path normalization, symlink identity, non-starting route assertion, exact-recipient broadcast with Set semantics, admission-failure explicit errors, aborted-client rejection. Wrapper 4/0 including survives-own-load, retire-on-orphan-load, duplicate-ready-in-window ignored, foreign-subscription non-forwarding, closed release. Full web suite 117/0. Plugin tsc 0 errors.
- EXACT TECHNICAL BLOCKER remaining: the supported browser builder (openclaw plugins build) refuses the entry - plugin entry does not expose tool or feature authoring metadata from dist/index.js - so the bundled dist/control-ui/hash/index.js artifact and the openclaw.plugin.json controlUi declaration cannot be generated yet. This aligns with Roots announced exact public SDK type import audit; the entry metadata shape it needs is the missing input. Raw tsc output remains in dist and the manifest has no controlUi field - not claimed as browser-ready.
- No runtime activation, deployment, or native action.

## REVIEWABLE BUILD CANDIDATE 2026-09-19 09:3x PDT
- Commit: fix(openclaw) real SDK types, nonce document identity, backend retirement, terminal project binding, browser build.
- REAL SDK: the complete installed 9.4 package is projected as an owned node_modules/openclaw symlink inside the integration; both ambient shims deleted; devDependencies declare openclaw 2026.9.4 and esbuild; EmbedHost, EmbedHostRequest, UiHandlerOptions, registration options, OperatorScope, and the request signature all derive from actual SDK exports; respond uses the vendor three-argument form with ErrorShape code AND message (frame helper, GSD_UI_* codes); tsc compiles 0 errors against the real declaration graph.
- DOCUMENT IDENTITY: per-negotiation nonce in the child ready ping, echoed by the wrapper bind, accepted only on exact match; wrong-nonce binds close transferred ports (no event-loop pinning); duplicate same-nonce ready ignored; fresh nonce retires and rebinds; load ordering never defines identity; async completions fenced by generation with late-subscribe release of the exact backend subscription; retirement releases owned subscriptions via tracked unsubscribe operations; unsubscribe forwarding gated on mount ownership.
- TERMINAL BINDING: PtySession identity binds to canonical project - hasSession(sessionId, projectCwd) compares realpaths, so admitted project A cannot select a project-B terminal; the stream route passes projectCwd under require_existing.
- BROWSER BUILD: package.json openclaw.controlUi points at ./src/control-ui-entry.ts; the entry attaches the SDK toolPluginMetadataSymbol authoring metadata (same shape defineToolPlugin publishes) to the feature-rich definePluginEntry; built via the SUPPORTED builder against an ISOLATED STAGING COPY at /home/openclaw/gsd-plugin-staging (outside plugins.load.paths, esbuild linked from the pnpm store): exit 0, bundle dist/control-ui/66b8b9244efa2559392ffc01c3039ef30551bbd26eb8de9e0a99ffb53fbe68c2/index.js (3295 bytes), openclaw.plugin.json controlUi descriptor written, --check exit 0 "Plugin metadata is up to date".
- TESTS by grep receipts: transport 16/0 including wrong-nonce rejection and port-closing; wrapper 4/0 nonce lifecycle with bind-echo assertions; integration 1/0 startup-to-first-request with nonce; ui-methods 7/0 real respond frames; full web suite 118 pass 0 fail; plugin tsc 0 errors.
- FIXES DURING THE ROUND: nonce null moved to teardown only (bind was erasing its own identity), bare parentMessages test references, port leak on rejected binds, esbuild sourced from the pnpm store.
- NO runtime activation, restart, reload, grant, or native action. The staging candidate and this receipt are submitted for activation review.

## TEN-ITEM REVIEW RESPONSE - COMPLETE CANDIDATE 2026-09-19 09:5x PDT
- Commit: e525c3dc fix(openclaw) ten-item review - qualified cleanup, project context, admission deadline, byte bounds, schema.
- ITEM 1: automatic retirement and late-generation release now use exact fully qualified gsd.ui. method names - proven by the strict host stub that rejects unknown Gateway methods.
- ITEM 2: teardown advances generation and the pending-subscription adoption tests disposed; late replies after disposal or generation change release THAT exact subscription. Document replacement and full mount disposal both tested.
- ITEM 3: terminal child mapping carries project (gate buildArgs reads the project param buildProjectUrl appends); server requireApprovedProject resolves the admitted project and the daemon URL includes project=canonicalRoot, validated against stored terminal identity by the realpath comparison.
- ITEM 4: picker browse path-only requests pin to the single approved project (never an arbitrary first of many); both browse and files mappings accept root-or-project; the save route PUT /api/preferences maps to gsd.ui.preferences.setDevRoot with a validated devRoot DTO proxying the daemon PUT.
- ITEM 5: streaming uses a clearable admission deadline - cleared once headers arrive so healthy streams are governed solely by the lifetime lease; the 10s timeout no longer kills live bodies.
- ITEM 6: release emits a targeted closed event on still-live connections; pre-admission exceptions answer the initial RPC with a structured error frame; exactly one initial response guaranteed across network exception, EOF, lease expiry, unsubscribe, stop, and disposal.
- ITEM 7: registerGsdUiMethods handle retained and disposeAll invoked on the gsd-web-portal service stop; policy REPLACED on every start/reload including explicit empty removal so withdrawn projects lose grants; preferences.read enforces admin admission consistently.
- ITEM 8: daemonFetch enforces the byte bound WHILE reading via the body reader - never after full buffering; DTO JSON-object validation retained.
- ITEM 9: authored metadata schema preserves webUi and admits the precise embeddedProjects policy (adminOnly, projectId+canonicalRoot items, additionalProperties false).
- ITEM 10: fresh web build BUILD_ID qmosXTOrTPgl7s0s_tjNt with standalone output timestamped 07:42:42 PDT - AFTER the 14:16/14:21 daemon/child source changes; staging refreshed and browser build regenerated: new bundle hash 695a0f80df1ec3bd7c66079515d59224211d498b651500afc71aea2ac9e483ff, manifest controlUi descriptor written, --check exit 0 Plugin metadata is up to date.
- TEST RECEIPTS by grep counts: ui-methods 9/0 including setDevRoot proxy, path-only pinning, closed-event delivery, admin-consistent preferences; wrapper 4/0 with the strict-host stub proving qualified cleanup and live-subscription retirement; gate 13/0 with the terminal project arg contract; transport 16/0; integration 1/0; full web suite 118 pass 0 fail; plugin tsc 0 errors against the real SDK.
- Fixes during the round: two intended-behavior test-expectation updates, one missing finally opener, one retire-test sequencing (closed-release before dispose), one terminal args contract update.
- NO runtime activation, restart, registry reload, auth change, or project grant. Complete browser and web candidates staged; submitted for activation review.

## HOLD 2026-09-19 09:50 PDT - root preparing corrective patches
- Root verified e525c3dc and the fresh qmos web build. Five remaining interop gaps named for root-bounded corrective patches in isolated worktrees: /api/boot lacks a gate host method; setDevRoot missing from the wrapper allowlist; Files root selector mistaken for project identity; pending admission disposal can suppress its initial reply; SSE bound still counts characters.
- Sole-writer hold in effect: no repair round, no rebuild, no checkout or staging writes from this lane until root returns reviewed patches and receipts. Integration/deployment ownership unchanged - patches will be integrated on arrival.

## INTEGRATION/ACTIVATION PLAN PREPARED 2026-09-19 10:2x PDT
- Plan at validation/sep19-integration-activation-plan.md: three-phase source integration from c98ebe5c, restart-free web handover with drain and rollback via the proven portal relaunch path, first Control UI registry admission behind a root-coordinated reload, and the identified single approved project (gsd-pi checkout, realpath-canonical, adminOnly default). No integration, reload, restart, or config change performed; e525 source/staging and live daemon held; awaiting root final web receipt and plan reconciliation.

## PLAN v2 CORRECTED 2026-09-19 10:4x PDT
- Corrections applied: merge-base ancestry (3 commits, not parent), actual projectId gsd-pi from the live state DB read-only, source-verified portal lifecycle (stop/start/cleanup/self-heal with exact spawn env and port verification), exact registry rebuild command openclaw plugins registry --refresh, dist-then-manifest publication ordering for partial-read safety, static-13:14 vs in-process daemon handover distinction, identity-based verification instead of full web suite reruns. Still held; no execution.

## FINAL CANDIDATE ACKNOWLEDGED 2026-09-19 14:2x CDT (single ack)
- Candidate verified read-only: web stage /home/openclaw/projects/gsd-web-stage-20260919-DARkBy/dist/web/standalone BUILD_ID 0iiBLMaE97sQIav9dXDT6, manifest SHA 90160772..., 2877 files / 248646780 bytes; plugin stage b8d01e8b browser bundle; receipts at integration .artifacts/integrated-final/ and plugin .candidate-final-receipt.json. Native overlays to PRESERVE on reproduction: @opengsd/engine-linux-x64-gnu 1.19.0 binary SHA 611e8c48... and pty.node 75728 bytes SHA d2e7a2fd...; failed 3.4MB med1ki candidate is quarantined-never-activate.
- Execution lifecycle plan remains validation/sep19-integration-activation-plan.md v2 (exact commands). Current rootID for the approved-project policy: projectId gsd-pi, canonicalRoot /home/openclaw/.openclaw/projects/gsd-pi (state-DB verified). Activation NOT claimed; hold maintained pending root reconciliation and an available native slot (Mac locked/dark wakes).

## ACTIVATION WINDOW v3 PREPARED 2026-09-19 16:5x CDT
- Root correction honored: registry --refresh is persisted-metadata-only; PluginServicesHandle.reload/stop are internal config-reload hooks; a planned web-child kill does not schedule start. v2 withdrawn.
- v3 artifact: /home/openclaw/gsd-activation-window-20260919/gsd-activation-window.sh - one-shot reviewable script, bash -n syntax-ok, 156 lines: PREPARE (identity checks vs candidate receipts incl native overlay SHAs, frozen-HEAD checks, abort-if-live-jobs/PTYs, rollback tree capture) / WINDOW STOP (graceful drain 330s bound) -> PUBLISH (web-root swap + plugin dist+manifest, old assets retained; atomicity from the process START, not copy ordering) / START (readyz poll 180s) / VERIFY (controlUi list + web tab route) / ROLLBACK (restore trees + restart + readyz, escalate on failure).
- Combined-window coordination to Main attempted (dedup-caveated); delivery unconfirmed at boundary.
- NOT EXECUTED. Decision points returned to root.

## SCRIPT v3.1 UNIT-FACT UPDATE 2026-09-19 16:5x CDT
- Root-proven unit facts applied: systemctl --user openclaw-gateway.service (FragmentPath user dir, jetkvm-env.conf drop-in, Type=simple, Restart=always, TimeoutStart=30s, TimeoutStop=330s, KillMode=mixed, KillSignal15). My earlier empty systemctl cat was a missing --user probe error, not unit absence - recorded. Script hard-gates publication on verified-stopped state (ActiveState inactive/failed AND readyz down); a single restart call is explicitly NOT treated as a publication interval. Combined-window preparation only; not executed.

## COMBINED WINDOW INTEGRATED 2026-09-19 17:0x CDT
- Main replied with exact lane commands, integrated into the v3.1 script as the WINDOW SERVE phase: SET openclaw config set gateway.tailscale.mode serve (single key, durable offline write, sequenced after the stopped-state gate and before publication/start) and ROLLBACK openclaw config set gateway.tailscale.mode off (explicit off, never unset). Main iPhone acceptance gates separately on connected:true after Bennett taps Retry - no embedded-browser auth claim.
- Main sequencing note (1) was stale: v3.1 already runs every systemctl on the user bus per root 16:48 unit proof; acknowledged to the record.
- One drain, one stop->serve+publish->start cycle, rollback tree per lane plus the explicit-off key. Not executed; awaiting root script review and external monitor availability.

## RECEIPT CORRECTION + COMBINED WINDOW INTEGRATED 2026-09-19 17:1x CDT
- CORRECTION: the previous entry claimed Serve-phase integration, but its python patch died at parse (unescaped apostrophes) before writing; the script was unchanged at that moment. The shell chain ran the receipt append anyway - recorded as a receipt-integrity mistake, now corrected.
- ACTUAL integration now landed and syntax-checked: WINDOW SERVE phase with Main exact commands (config set gateway.tailscale.mode serve after the stopped-state gate, verified via config get; rollback sets explicit off, never unset), window dispatch stop->serve->publish->start->verify.
- Main sequencing note (1) was stale: v3.1 already uses systemctl --user per root unit proof.
- Not executed; awaiting root review and external monitor availability.

## SCRIPT RETIRED / OWNERSHIP SPLIT 2026-09-19 16:5x CDT
- gsd-activation-window.sh retired as an UNAPPROVED DRAFT per root review: per-run TS fragmenting backup generations, no post-stop failure trap, && chain suppressing errexit, webroot-path.txt never captured, stale port/process heuristics not proving quiescence, swallowed cp failures, declared-but-unused manifest SHA, absent source fast-forward and concrete config edits, unproven CLI verify, raw Bearer request not establishing trusted-proxy auth. No further scripted automation rounds.
- OWNERSHIP: root owns LIVE maintenance execution as external hands - step-by-step user-unit stop, verified stopped state, controlled reviewed publication, start, real gateway/native checks in one stable awake window. 3eae retains reviewed GSD candidate/source handback ownership. Main retains the exact config proposal.
- Corrected facts: live web cwd /home/openclaw/.openclaw/projects/gsd-pi/dist/web/standalone for PID 1725535 (revalidate at window time); supported catalog check is Gateway RPC plugins.controlUi.list; my earlier stop probe was cat/status only - no stop ever ran, gateway PID 1235411 healthy throughout.
- Send discipline correction accepted: uncertain internal sends are not to be repeated with dedup caveats - reconcile durable transcript/run IDs first.
- All live state, original e525, and candidates frozen until the window; existing rollback assets preserved. No new implementation required.

## MAIN COORDINATION CLOSED 2026-09-19 17:0x CDT
- Durable reconciliation per root send-discipline rule: Main single proposal confirmed at runId 6b97a069 (their dedup note); my duplicated sends are superseded by that ID. Commands unchanged and already recorded: SET openclaw config set gateway.tailscale.mode serve (lands while gateway down, loads at start); ROLLBACK explicit off, never unset. Root now owns combined-window sequencing; both lanes preparation-only. Corrected plan: drafts/2026-09-19-iphone-managed-serve-recovery-plan.md section 6.

## ROOT MAINTENANCE EXECUTED - CHECKPOINT ACKNOWLEDGED 2026-09-19 17:2x CDT
- Window completed by root: exact 9.4 gateway PID 2409959 / NRestarts0 ready since 22:17 UTC; source e525 -> c98ebe5c fast-forward; coherent full web/plugin/generated manifest plus exact 2 config fields published; fixed backup at /home/openclaw/gsd-maintenance-20260919-2215. Mac auto-reconnected; managed Serve HTTPS 443 readyz 200 verified FROM MAC; native Workboard healthy. iPhone still disconnected (cached credential / live Retry unproven).
- Vendor stop drain settled but exited 1 on an old service-child anchor closing-receipt failure; old processes gone before publication, no continuing recurrence recorded.
- Auto-update unexpectedly enabled with countdown after startup: root set ONLY update.auto.enabled=false at 22:18, hot-reload applied, 9.4 remains. VERSION-COUPLING HOLD accepted: no doctor/update/restart/restore-auto-update while the repaired UI is 9.4.
- New registered GSD wrapper: Welcome and Prefs work; Browse returns no approved project even for exact gsd-pi (config policy verified correct: adminOnly / one gsd-pi entry). Legacy first GSD pin remains direct-HTTP Load failed; second pin is the new wrapper. GSD ACCEPTANCE NOT COMPLETE - root is tracing policy-reference/registration lifecycle with read-only workers and owns all writes.
- This lane: no mutate/restart/rebuild/replay; candidate/source handback ownership continues.

## ROOT RECOVERY CHECKPOINT ACKNOWLEDGED 2026-09-19 17:5x CDT (22:58 UTC)
- Gateway 2451815 exact 2026.9.4 / NRestarts0 healthy; two subsequent graceful windows clean, anchor-receipt failure did not recur. Managed private Tailscale Serve 443 restored; iPhone host vado.tail6d6d37.ts.net/443/TLS existing pairing awaiting user live Retry. update.auto.enabled=false retained (custom repaired UI is 9.4).
- Current identities: GSD source 87156e9f, registration plugin 82f90728, web BUILD 1tZI9GMpMbjtAaklzq4FF (root lanes advanced beyond frozen c98ebe5c). NATIVE PASS: Mac one GSD link, approved picker, full Dashboard and Bridge connected; storage crash fixed.
- Remaining gap: Files reveals truly missing GET /api/files bridge operation; root worker preparing narrow read-only files.read tree/content under existing project/profile/admin fences. Root sole live executor; 3eae source ownership frozen - no duplicate build/restart/auth/blind replay. Latest complete rollback /home/openclaw/gsd-web-storage-maintenance-20260919.
- STRANDED-WORK CHECK by this lane: none new - no exec/subagent/media tasks in flight; candidates consumed by executed windows; receipts and plans recorded; intentionally parked items unchanged (upstream core portal-origin issue awaiting Bennett go; two stale groupmetoolkit TaskFlow trackers parked on GSD events).

## FINAL NATIVE RECOVERY HANDBACK - ACCEPTED AND CONSUMED 2026-09-19 18:1x CDT (23:12 UTC)
- Identities verified read-only by source owner: original checkout AND root worktree both at 195e9eb2 (fix openclaw bridge approved project file reads); original dirty state is exactly the generated openclaw.plugin.json plus pre-existing untracked IDENTITY/SOUL/USER files; receipts present at /home/openclaw/gsd-files-read-maintenance-20260919.
- Runtime received: gateway 2470719 exact 2026.9.4 NRestarts0 healthy; local + private Tailscale HTTPS readyz 200 FROM MAC; final drain clean 23:08:29.291/4802ms with old gateway/GSD/bridge gone before publication. Matched plugin manifest 6d5225a1..., browser generation 7db7bfcf, web BUILD 7PowhQrf6KoMVSw_owkh8.
- NATIVE ACCEPTANCE RECORDED: one GSD nav; approved picker; complete Dashboard and Bridge PASS; Files .gsd tree and automatic STATE.md render PASS; project tree and root-clicked README.md render PASS; no AI prompt, initialization, step, write, delete, or model invocation; acceptance session 01a0bbef is normal idle managed bridge; Workboard native Refresh and prior official widget interaction PASS.
- Bounded gaps acknowledged and NOT accepted by design: Files POST save/duplicate, PUT create, PATCH rename remain unmapped; generic separate-port portals remain a separate ingress limitation. update.auto.enabled=false hold continues while custom UI is 9.4. iPhone existing-paired device still needs Bennett live Retry (Tailscale connected, vado.tail6d6d37.ts.net, 443, TLS, existing credentials).
- STRANDED EXECUTION CHECK: none - no exec sessions, subagents, or media tasks in flight in this lane.
- Lane resumption: existing coordinated scope continues FROM 195e9eb2 with no duplicate activation/restart and no overwriting of root changes. Root keeps quiet UI monitoring; Main/Overseer normal work, no blind historical replays.
